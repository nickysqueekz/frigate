name: Build and Push Frigate to GHCR

on:
  push:
    branches:
      - dev

env:
  IMAGE_NAME: frigate

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.CR_PAT }}

      - name: Extract short commit
        id: vars
        run: echo "COMMIT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/frigate-coral-x64.Dockerfile  # ✅ Your optimized Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ secrets.GHCR_USERNAME }}/frigate:0.16-dev
            ghcr.io/${{ secrets.GHCR_USERNAME }}/frigate:0.16-dev-${{ env.COMMIT_SHA }}
            ghcr.io/${{ secrets.GHCR_USERNAME }}/frigate:latest
